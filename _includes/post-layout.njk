---
layout: base-layout.njk
eleventyComputed:
  pagetitle: "{{ title }}"
  pagedesc: "{{ page.excerpt | trim }}"
---
<article>
<h1>{{ title }}</h1> 
<time datetime="{{ date | dateIso }}">{{ date | dateReadable }}</time> 
{{ content | safe }}</article>